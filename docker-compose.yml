version: '3'
services:
  postgres:
    image: "postgres"
    environment:
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=root
      - POSTGRES_DB=root      
    ports:
      - "5436:5432"
    volumes:
      - ./postgres_data:/var/lib/postgres

volumes:
  postgres_data: